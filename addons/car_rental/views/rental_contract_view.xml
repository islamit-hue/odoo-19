<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- List view -->
    <record id="view_rental_contract_list" model="ir.ui.view">
        <field name="name">rental.contract.list</field>
        <field name="model">rental.contract</field>
        <field name="arch" type="xml">
            <list string="عقود التأجير"
                  decoration-info="state == 'draft'"
                  decoration-success="state == 'done'"
                  decoration-warning="state == 'active'"
                  decoration-danger="state == 'cancelled'">
                <field name="name"/>
                <field name="customer_id"/>
                <field name="car_id"/>
                <field name="date_from"/>
                <field name="date_to"/>
                <field name="total_days"/>
                <field name="total_amount"/>
                <field name="state" widget="badge"/>
            </list>
        </field>
    </record>

    <!-- Form view -->
    <record id="view_rental_contract_form" model="ir.ui.view">
        <field name="name">rental.contract.form</field>
        <field name="model">rental.contract</field>
        <field name="arch" type="xml">
            <form string="عقد تأجير">
                <header>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,confirmed,active,done"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group string="معلومات العقد">
                            <field name="customer_id"/>
                            <field name="car_id"/>
                        </group>
                        <group string="تواريخ التأجير">
                            <field name="date_from"/>
                            <field name="date_to"/>
                            <field name="total_days"/>
                            <field name="total_amount"/>
                        </group>
                    </group>
                    <group string="ملاحظات">
                        <field name="note" nolabel="1" placeholder="أضف ملاحظات هنا..."/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_rental_contract" model="ir.actions.act_window">
        <field name="name">عقود التأجير</field>
        <field name="res_model">rental.contract</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                أنشئ أول عقد تأجير!
            </p>
        </field>
    </record>
</odoo>
