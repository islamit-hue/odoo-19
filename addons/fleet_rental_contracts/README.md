# Fleet Rental Contracts

This module implements car rental contracts, checkout/checkin checklists, and damage logging for Odoo 19.

### Configuration

To configure the database password, replace it with a placeholder in your `odoo.conf` file:

```ini
[options]
db_password = your_db_password_here
```

Remember to set your actual password when deploying locally.